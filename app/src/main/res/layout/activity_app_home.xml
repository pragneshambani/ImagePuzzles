<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.sumit.animalplanet.AppHomeActivity">

    <TextView
        android:id="@+id/pageTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:paddingLeft="5dp"
        android:textSize="30dp"
        android:text="Image Puzzle"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/BLACK"
        android:layout_below="@id/pageTitle"
        android:layout_marginTop="1dp"
        />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/pageTitle"
        android:layout_alignBottom="@+id/pageTitle"
        android:layout_alignRight="@+id/divider"
        android:layout_marginLeft="300dp"
        android:layout_marginBottom="2dp"
        android:adjustViewBounds="true"
        android:src="@drawable/settings"
        android:foregroundGravity="bottom"
        android:onClick="showSettings"
        />
    <Button
        android:id="@+id/toAPGameButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Animal Planet"
        android:onClick="onAPButtonTap"/>

    <Button
        android:id="@+id/toCWGameButton"
        android:layout_below="@+id/toAPGameButton"
        android:layout_alignLeft="@+id/toAPGameButton"
        android:layout_alignRight="@+id/toAPGameButton"
        android:paddingTop="5dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Color World"
        android:onClick="onCWButtonTap"/>

</RelativeLayout>
